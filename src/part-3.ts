/*
 * 1. Insertion Sort
 *
 * Insertion works by iteratively inserting each element of an unsorted list into its
 * correct position in a sorted portion of the list.
 *
 * Insertion sort is like sorting playing cards in your hands. You split the cards into
 * two groups: the sorted cards and the unsorted cards. Then, you pick a card from the
 * unsorted group and put it in the right place in the sorted group.
 *
 * Don't restrict the array to just numbers and don't forget to type the return value!
 */

const insertionSort = (array) => {};

// console.log(insertionSort([6, 2, 4, 3, 5, 1])); // [1, 2, 3, 4, 5, 6]

/*
 * 2. Bubble Sort
 *
 * Bubble sort works by repeatedly swapping adjacent elements if they are in the wrong
 * order.
 *
 * Don't restrict the array to just numbers and don't forget to type the return value!
 */

const bubbleSort = (array) => {};

// console.log(bubbleSort([6, 2, 4, 3, 5, 1])); // [1, 2, 3, 4, 5, 6]

/*
 * 3. Merge Sort
 *
 * Merge sort follows a divide-and-conquer approach. It works by recursively dividing
 * the input array into smaller sub-arrays, sorting those sub-arrays, and then merging
 * them back together to obtain the sorted array.
 *
 * Don't restrict the array to just numbers and don't forget to type the return value!
 */

const mergeSort = (array) => {};

// console.log(mergeSort([6, 2, 8, 4, 3, 7, 5, 1])); // [1, 2, 3, 4, 5, 6, 7, 8]

/* don't edit this line! */
export { insertionSort, bubbleSort, mergeSort };
